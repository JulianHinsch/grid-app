import React from 'react';
import { render } from '@testing-library/react';
import NewResourceDialog from '../components/NewResourceDialog/NewResourceDialog';

it('Does not allow submit with empty fields', () => {
    // TODO
});
